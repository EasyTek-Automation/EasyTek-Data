# Ambiente virtual
.venv/

# Arquivo de variáveis de ambiente
.env

# Ferramentas locais
tools/
backups/

# Arquivos temporários/excluídos
*.py[cod]
__pycache__/
*.log
*.tmp
*~
*.swp

requirements copy
ReunirProjeto
docs/

# --- Arquivos de Ambiente e Credenciais (NUNCA COMITAR) --- 
.env 
*.env.*  
# --- Dados de Serviços Docker (NUNCA COMITAR) --- # Contém certificados SSL e configurações do proxy 
/nginx-proxy-manager/  
# --- Dependências e Pacotes --- # Python 
.venv/ 
*.egg-info/ 
pip-wheel-metadata/  
# Node.js (para o Node-RED) 
/node-red-config/node_modules/  
# --- Arquivos de Cache e Sistema Operacional --- 
__pycache__/ 
*.pyc 
.DS_Store 
Thumbs.db  
# --- Arquivos de Log e Temporários --- 
*.log 
*.log.* 
*.tmp 
*~ 
*.swp  
# --- Arquivos de IDE --- 
.vscode/ 
.idea/  
# --- Backups e Dados (se não forem parte do código-fonte) --- 
/Backups/ 
/data/ 
/cache/

  GNU nano 4.8                                                                                                      .gitignore                                                                                                      Modified # Ambiente virtual .venv/  # Arquivo de variáveis de ambiente .env  # Ferramentas locais tools/ backups/  # Arquivos temporários/excluídos *.py[cod] __pycache__/ *.log *.tmp *~ *.swp  requirements copy ReunirProjeto docs/  # --- Arquivos de Ambiente e Credenciais (NUNCA COMITAR) --- .env *.env.* # --- Dados de Serviços Docker (NUNCA COMITAR) --- # Contém certificados SSL e configurações do proxy /nginx-proxy-manager/ # --- Dependências e Pacotes --- # Python .venv/ *.egg-info/ pip-wheel-metadata/ # Node.js (para o Node-RED) /node-red-config/node_modules/ # --- Arquivos de Cache e Sistema Operacional --- __pycache__/ *.pyc .DS_Store Thumbs.db # --- Arquivos de Log e Temporários --- *.log *.log.* *.tmp *~ *.swp # --- Arquivos de IDE --- .vscode/ .idea/ # --- Backups e Dados (se não forem parte do código-fonte) --- /Backups/ /data/ /cache/
